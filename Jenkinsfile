pipeline {
  agent any
  stages {
    stage('Build API') {
      steps {
        echo 'Building API'
      }
    }
    stage('Deploy API') {
      steps {
        echo 'Deploying API'
      }
    }
  }
}